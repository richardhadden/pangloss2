[project]
name = "pangloss"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Richard Hadden", email = "richard.hadden@oeaw.ac.at" }
]
requires-python = ">=3.14"
dependencies = [
    "frozendict>=2.4.7",
    "pydantic>=2.12.5",
    "pydantic-meta-kit",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "pygount>=3.2.0",
    "pytest>=9.0.2",
    "pytest-sugar>=1.1.1",
    "ty>=0.0.21",
]

[tool.uv.workspace]
members = [
    "pangloss/pydantic_meta_kit",
]

[tool.uv.sources]
pydantic-meta-kit = { git = "https://github.com/richardhadden/pydantic-meta-kit.git" }
